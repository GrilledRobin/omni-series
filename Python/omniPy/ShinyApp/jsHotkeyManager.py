#!/usr/bin/env python3
# -*- coding: utf-8 -*-

from inspect import cleandoc

def jsHotkeyManager(
    funcName : str = 'HotkeyManager'
) -> str:
    #000. Info.
    '''
#---------------------------------------------------------------------------------------------------------------------------------------#
#100.   Introduction.                                                                                                                   #
#---------------------------------------------------------------------------------------------------------------------------------------#
#   |This function is intended to create a universal hotkey manager in the HTML App, not necessarily `shiny` App, to manage the hotkeys #
#   | for various components                                                                                                            #
#   |-----------------------------------------------------------------------------------------------------------------------------------#
#   |FEATURE                                                                                                                            #
#   |-----------------------------------------------------------------------------------------------------------------------------------#
#   |[1] Register/unregister hotkeys for any web component, to resemble actions of certain `handlers`                                   #
#   |[2] There can be more than one hotkey registered for the same component                                                            #
#   |[3] The same hotkey can be registered to many components, while executing the `handlers` in the registration sequence, although it #
#   |    is highly NOT recommended as it is difficult to detect the issues if any                                                       #
#   |-----------------------------------------------------------------------------------------------------------------------------------#
#   |ASSUMPTION                                                                                                                         #
#   |-----------------------------------------------------------------------------------------------------------------------------------#
#   |[1] With this JS injection, the dedicated web component, preferrably a `button`, becomes able to accept keyboard shortcut to       #
#   |     resemble its `click` event                                                                                                    #
#   |[2] Duplicated decorators, i.e. `ctrl+ctrl+A` is not accepted and hence cannot be registered as a hotkey                           #
#   |[3] Accecpted modifier keys are <'ctrl','alt','shift','command','option','meta'> in Windows and MacOS                              #
#   |[4] Modifiers are optional, hence a single letter can be a hotkey                                                                  #
#   |[5] Modifier itself cannot be the hotkey                                                                                           #
#---------------------------------------------------------------------------------------------------------------------------------------#
#200.   Methods                                                                                                                         #
#---------------------------------------------------------------------------------------------------------------------------------------#
#   |100.   Public method                                                                                                               #
#   |-----------------------------------------------------------------------------------------------------------------------------------#
#   |   |-------------------------------------------------------------------------------------------------------------------------------#
#   |   |[constructor]                                                                                                                  #
#   |   |-------------------------------------------------------------------------------------------------------------------------------#
#   |   |   |001.   Introduction.                                                                                                       #
#   |   |   |---------------------------------------------------------------------------------------------------------------------------#
#   |   |   |   |This method is intended to instantiate the `JS` class                                                                  #
#   |   |   |---------------------------------------------------------------------------------------------------------------------------#
#   |   |   |100.   Parameters.                                                                                                         #
#   |   |   |---------------------------------------------------------------------------------------------------------------------------#
#   |   |   |options           :   <Object   > Universal configurations of the manager. Keys are as below                               #
#   |   |   |                      |----------------------------------------------------------------------------------------------------#
#   |   |   |                      |key             |Type     |Default    |Description                                                  #
#   |   |   |                      |----------------+---------+-----------+-------------------------------------------------------------#
#   |   |   |                      |ignoreEditable  |Boolean  |true       | Whether to ignore `Editable` element of the `HTML`, when    #
#   |   |   |                      |                |         |           |  `false`, hotkeys can still take effect when the focus is in#
#   |   |   |                      |                |         |           |  an editable element, e.g. `input box`. As convention, when #
#   |   |   |                      |                |         |           |  current focus is in an `editable`, the hotkey does not     #
#   |   |   |                      |                |         |           |  take effect for it can be the input content to the element #
#   |   |   |                      |preventDefault  |Boolean  |true       | Whether to prevent the default behavior of the web browser  #
#   |   |   |                      |                |         |           |  when pressing the hotkeys, preferred `true` for all        #
#   |   |   |                      |                |         |           |  customized event handlers                                  #
#   |   |   |                      |stopPropagation |Boolean  |true       | Whether to prevent the hotkey pressing from bubbling to the #
#   |   |   |                      |                |         |           |  global environment, preferred `true` for all customized    #
#   |   |   |                      |                |         |           |  event handlers                                             #
#   |   |   |                      |ignoreRepeat    |Boolean  |true       | Whether to ignore the `event.repeat` situation. This is not #
#   |   |   |                      |                |         |           | for the case when clicking several times of the hotkeys in  #
#   |   |   |                      |                |         |           | a short period of time, unless it is defined elsewhere.     #
#   |   |   |                      |debug           |Boolean  |false      | Switch to `debug` mode to check the output in the console   #
#   |   |   |                      |description     |String   |null       | Can be ignored during construction, but useful when calling #
#   |   |   |                      |                |         |           | `register` in debug mode                                    #
#   |   |   |                      |----------------+---------+-----------+-------------------------------------------------------------#
#   |   |   |---------------------------------------------------------------------------------------------------------------------------#
#   |   |   |900.   Return Values by position.                                                                                          #
#   |   |   |---------------------------------------------------------------------------------------------------------------------------#
#   |   |   |<null>            :   Only for initialization                                                                              #
#   |   |   |---------------------------------------------------------------------------------------------------------------------------#
#   |   |-------------------------------------------------------------------------------------------------------------------------------#
#   |   |[register]                                                                                                                     #
#   |   |-------------------------------------------------------------------------------------------------------------------------------#
#   |   |   |001.   Introduction.                                                                                                       #
#   |   |   |---------------------------------------------------------------------------------------------------------------------------#
#   |   |   |   |This method is intended to register a hotkey to the specified element, which triggers the `handler`                    #
#   |   |   |---------------------------------------------------------------------------------------------------------------------------#
#   |   |   |100.   Parameters.                                                                                                         #
#   |   |   |---------------------------------------------------------------------------------------------------------------------------#
#   |   |   |component         :   <Object   > Web element for the hotkey to bind, e.g. obtained by `QuerySelector()`                   #
#   |   |   |hotkeyString      :   <String   > String as hotkey to parse and then bind to the `component`                               #
#   |   |   |handler           :   <Function > Function as callback to process when the hotkey event is triggered                       #
#   |   |   |                      [IMPORTANT] It takes only one argument as the `event` to handle, where `event` is not related to the #
#   |   |   |                                  internal logic of `handler`, but is used by the private method `#handleKeyDown`          #
#   |   |   |options           :   <Object   > Specific `options` that overrides the universal one for this registration, see available #
#   |   |   |                      keys in the `constructor` part                                                                       #
#   |   |   |---------------------------------------------------------------------------------------------------------------------------#
#   |   |   |900.   Return Values by position.                                                                                          #
#   |   |   |---------------------------------------------------------------------------------------------------------------------------#
#   |   |   |<Function>        :   Return a function to `unregister` the hotkey that is just registerred to the `component`, useful to  #
#   |   |   |                      remove the binding to this specific `component` at runtime                                           #
#   |   |   |---------------------------------------------------------------------------------------------------------------------------#
#   |   |-------------------------------------------------------------------------------------------------------------------------------#
#   |   |[unregister]                                                                                                                   #
#   |   |-------------------------------------------------------------------------------------------------------------------------------#
#   |   |   |001.   Introduction.                                                                                                       #
#   |   |   |---------------------------------------------------------------------------------------------------------------------------#
#   |   |   |   |This method is intended to unbind a hotkey from the specific `component`, regardless of whether the hotkey is already  #
#   |   |   |   | registered to that `component`                                                                                        #
#   |   |   |---------------------------------------------------------------------------------------------------------------------------#
#   |   |   |100.   Parameters.                                                                                                         #
#   |   |   |---------------------------------------------------------------------------------------------------------------------------#
#   |   |   |component         :   <Object   > Web element for the hotkey to unbind, e.g. obtained by `QuerySelector()`                 #
#   |   |   |hotkeyString      :   <String   > String as hotkey to parse and then unbind from the `component`                           #
#   |   |   |                      [IMPORTANT] If it is NOT provided, all hotkeys of the `component` will be removed                    #
#   |   |   |---------------------------------------------------------------------------------------------------------------------------#
#   |   |   |900.   Return Values by position.                                                                                          #
#   |   |   |---------------------------------------------------------------------------------------------------------------------------#
#   |   |   |<Number>          :   Return the number of hotkeys that have been removed from the `component`                             #
#   |   |   |---------------------------------------------------------------------------------------------------------------------------#
#   |   |-------------------------------------------------------------------------------------------------------------------------------#
#   |   |[unregisterAll]                                                                                                                #
#   |   |-------------------------------------------------------------------------------------------------------------------------------#
#   |   |   |001.   Introduction.                                                                                                       #
#   |   |   |---------------------------------------------------------------------------------------------------------------------------#
#   |   |   |   |This method is intended to remove all registered hotkeys from the global environment                                   #
#   |   |   |---------------------------------------------------------------------------------------------------------------------------#
#   |   |   |100.   Parameters.                                                                                                         #
#   |   |   |---------------------------------------------------------------------------------------------------------------------------#
#   |   |   |<null>            :   This function does not take parameters                                                               #
#   |   |   |---------------------------------------------------------------------------------------------------------------------------#
#   |   |   |900.   Return Values by position.                                                                                          #
#   |   |   |---------------------------------------------------------------------------------------------------------------------------#
#   |   |   |<Number>          :   Return the number of hotkeys that have been removed from the global environment                      #
#   |   |   |---------------------------------------------------------------------------------------------------------------------------#
#   |   |-------------------------------------------------------------------------------------------------------------------------------#
#   |   |[pause]                                                                                                                        #
#   |   |-------------------------------------------------------------------------------------------------------------------------------#
#   |   |   |001.   Introduction.                                                                                                       #
#   |   |   |---------------------------------------------------------------------------------------------------------------------------#
#   |   |   |   |This method is intended to pause the event listening so that all hotkeys will be deactivated temporarily, useful when  #
#   |   |   |   | there are modals or popup dialogs                                                                                     #
#   |   |   |---------------------------------------------------------------------------------------------------------------------------#
#   |   |   |100.   Parameters.                                                                                                         #
#   |   |   |---------------------------------------------------------------------------------------------------------------------------#
#   |   |   |<null>            :   This function does not take parameters                                                               #
#   |   |   |---------------------------------------------------------------------------------------------------------------------------#
#   |   |   |900.   Return Values by position.                                                                                          #
#   |   |   |---------------------------------------------------------------------------------------------------------------------------#
#   |   |   |<null>            :   This method only deactivates the hotkeys                                                             #
#   |   |   |---------------------------------------------------------------------------------------------------------------------------#
#   |   |-------------------------------------------------------------------------------------------------------------------------------#
#   |   |[resume]                                                                                                                       #
#   |   |-------------------------------------------------------------------------------------------------------------------------------#
#   |   |   |001.   Introduction.                                                                                                       #
#   |   |   |---------------------------------------------------------------------------------------------------------------------------#
#   |   |   |   |This method is intended to resume the event listening from being `paused` so that all hotkeys will be reactivated      #
#   |   |   |---------------------------------------------------------------------------------------------------------------------------#
#   |   |   |100.   Parameters.                                                                                                         #
#   |   |   |---------------------------------------------------------------------------------------------------------------------------#
#   |   |   |<null>            :   This function does not take parameters                                                               #
#   |   |   |---------------------------------------------------------------------------------------------------------------------------#
#   |   |   |900.   Return Values by position.                                                                                          #
#   |   |   |---------------------------------------------------------------------------------------------------------------------------#
#   |   |   |<null>            :   This method only reactivates the hotkeys                                                             #
#   |   |   |---------------------------------------------------------------------------------------------------------------------------#
#   |   |-------------------------------------------------------------------------------------------------------------------------------#
#   |   |[isPaused]                                                                                                                     #
#   |   |-------------------------------------------------------------------------------------------------------------------------------#
#   |   |   |001.   Introduction.                                                                                                       #
#   |   |   |---------------------------------------------------------------------------------------------------------------------------#
#   |   |   |   |This method is intended to identify the event listening status of the hotkeys                                          #
#   |   |   |---------------------------------------------------------------------------------------------------------------------------#
#   |   |   |100.   Parameters.                                                                                                         #
#   |   |   |---------------------------------------------------------------------------------------------------------------------------#
#   |   |   |<null>            :   This function does not take parameters                                                               #
#   |   |   |---------------------------------------------------------------------------------------------------------------------------#
#   |   |   |900.   Return Values by position.                                                                                          #
#   |   |   |---------------------------------------------------------------------------------------------------------------------------#
#   |   |   |<null>            :   This method only identify the activation status the hotkeys                                          #
#   |   |   |---------------------------------------------------------------------------------------------------------------------------#
#   |   |-------------------------------------------------------------------------------------------------------------------------------#
#   |   |[getRegisteredHotkeys]                                                                                                         #
#   |   |-------------------------------------------------------------------------------------------------------------------------------#
#   |   |   |001.   Introduction.                                                                                                       #
#   |   |   |---------------------------------------------------------------------------------------------------------------------------#
#   |   |   |   |This method is intended to obtain all the registered hotkeys from current instance                                     #
#   |   |   |---------------------------------------------------------------------------------------------------------------------------#
#   |   |   |100.   Parameters.                                                                                                         #
#   |   |   |---------------------------------------------------------------------------------------------------------------------------#
#   |   |   |<null>            :   This function does not take parameters                                                               #
#   |   |   |---------------------------------------------------------------------------------------------------------------------------#
#   |   |   |900.   Return Values by position.                                                                                          #
#   |   |   |---------------------------------------------------------------------------------------------------------------------------#
#   |   |   |<Object>          :   The collection of all hotkeys, including below                                                       #
#   |   |   |                      [hotkeyString       ] The registered hotkey, which is parsed and standardized in format              #
#   |   |   |                      [count              ] How many elements has this hotkey been bound to                                #
#   |   |   |                      [descriptions       ] The `description` for these hotkeys when `register`ing                         #
#   |   |   |---------------------------------------------------------------------------------------------------------------------------#
#   |   |-------------------------------------------------------------------------------------------------------------------------------#
#   |   |[getRegistrationCount]                                                                                                         #
#   |   |-------------------------------------------------------------------------------------------------------------------------------#
#   |   |   |001.   Introduction.                                                                                                       #
#   |   |   |---------------------------------------------------------------------------------------------------------------------------#
#   |   |   |   |This method is intended to obtain the number of registered hotkeys in current instance                                 #
#   |   |   |---------------------------------------------------------------------------------------------------------------------------#
#   |   |   |100.   Parameters.                                                                                                         #
#   |   |   |---------------------------------------------------------------------------------------------------------------------------#
#   |   |   |<null>            :   This function does not take parameters                                                               #
#   |   |   |---------------------------------------------------------------------------------------------------------------------------#
#   |   |   |900.   Return Values by position.                                                                                          #
#   |   |   |---------------------------------------------------------------------------------------------------------------------------#
#   |   |   |<Number>          :   Return the number of hotkeys that have been registered in current instance                           #
#   |   |   |---------------------------------------------------------------------------------------------------------------------------#
#   |   |-------------------------------------------------------------------------------------------------------------------------------#
#   |   |[dispose]                                                                                                                      #
#   |   |-------------------------------------------------------------------------------------------------------------------------------#
#   |   |   |001.   Introduction.                                                                                                       #
#   |   |   |---------------------------------------------------------------------------------------------------------------------------#
#   |   |   |   |This method is intended to dispose current manager instance and recollect all system resources to prevent RAM leak     #
#   |   |   |---------------------------------------------------------------------------------------------------------------------------#
#   |   |   |100.   Parameters.                                                                                                         #
#   |   |   |---------------------------------------------------------------------------------------------------------------------------#
#   |   |   |<null>            :   This function does not take parameters                                                               #
#   |   |   |---------------------------------------------------------------------------------------------------------------------------#
#   |   |   |900.   Return Values by position.                                                                                          #
#   |   |   |---------------------------------------------------------------------------------------------------------------------------#
#   |   |   |<null>            :   It only conduct garbage collection process                                                           #
#   |   |   |---------------------------------------------------------------------------------------------------------------------------#
#   |   |-------------------------------------------------------------------------------------------------------------------------------#
#   |-----------------------------------------------------------------------------------------------------------------------------------#
#   |400.   Private method                                                                                                              #
#   |-----------------------------------------------------------------------------------------------------------------------------------#
#   |   |-------------------------------------------------------------------------------------------------------------------------------#
#   |   |[#normalizeKey]                                                                                                                #
#   |   |-------------------------------------------------------------------------------------------------------------------------------#
#   |   |   |001.   Introduction.                                                                                                       #
#   |   |   |---------------------------------------------------------------------------------------------------------------------------#
#   |   |   |   |This method is intended to standardize the hotkey for internal usage                                                   #
#   |   |   |---------------------------------------------------------------------------------------------------------------------------#
#   |   |   |100.   Parameters.                                                                                                         #
#   |   |   |---------------------------------------------------------------------------------------------------------------------------#
#   |   |   |rawKey            :   <String   > The extracted raw input during `#parseHotkeyString`                                      #
#   |   |   |---------------------------------------------------------------------------------------------------------------------------#
#   |   |   |900.   Return Values by position.                                                                                          #
#   |   |   |---------------------------------------------------------------------------------------------------------------------------#
#   |   |   |<String>          :   The standardized string                                                                              #
#   |   |   |---------------------------------------------------------------------------------------------------------------------------#
#   |   |-------------------------------------------------------------------------------------------------------------------------------#
#   |   |[#parseHotkeyString]                                                                                                           #
#   |   |-------------------------------------------------------------------------------------------------------------------------------#
#   |   |   |001.   Introduction.                                                                                                       #
#   |   |   |---------------------------------------------------------------------------------------------------------------------------#
#   |   |   |   |This method is intended to parse the provided string into valid hotkey collection                                      #
#   |   |   |---------------------------------------------------------------------------------------------------------------------------#
#   |   |   |100.   Parameters.                                                                                                         #
#   |   |   |---------------------------------------------------------------------------------------------------------------------------#
#   |   |   |hotkeyString      :   <String   > String as hotkey to parse and create a unique signature                                  #
#   |   |   |---------------------------------------------------------------------------------------------------------------------------#
#   |   |   |900.   Return Values by position.                                                                                          #
#   |   |   |---------------------------------------------------------------------------------------------------------------------------#
#   |   |   |<Set>             :   The `set` of objects as below (without names)                                                        #
#   |   |   |                      [standardKey        ] The standardized hotkey string                                                 #
#   |   |   |                      [modifierSet        ] The `set` of modifiers, e.g. `{'ctrl','alt'}` for a hotkey `'ctrl+alt+f'`      #
#   |   |   |                      [normalizedSignature] The unique signature in current instance                                       #
#   |   |   |---------------------------------------------------------------------------------------------------------------------------#
#   |   |-------------------------------------------------------------------------------------------------------------------------------#
#   |   |[#safeParse]                                                                                                                   #
#   |   |-------------------------------------------------------------------------------------------------------------------------------#
#   |   |   |001.   Introduction.                                                                                                       #
#   |   |   |---------------------------------------------------------------------------------------------------------------------------#
#   |   |   |   |This method is intended to parse the provided string into valid hotkey collection, or return `null` if any errors      #
#   |   |   |---------------------------------------------------------------------------------------------------------------------------#
#   |   |   |100.   Parameters.                                                                                                         #
#   |   |   |---------------------------------------------------------------------------------------------------------------------------#
#   |   |   |hotkeyString      :   <String   > String as hotkey to parse and create a unique signature                                  #
#   |   |   |---------------------------------------------------------------------------------------------------------------------------#
#   |   |   |900.   Return Values by position.                                                                                          #
#   |   |   |---------------------------------------------------------------------------------------------------------------------------#
#   |   |   |<Set>             :   The same object as `#parseHotkeyString` returns or `null` when encountering any errors               #
#   |   |   |---------------------------------------------------------------------------------------------------------------------------#
#   |   |-------------------------------------------------------------------------------------------------------------------------------#
#   |   |[#removeFromRegistry]                                                                                                          #
#   |   |-------------------------------------------------------------------------------------------------------------------------------#
#   |   |   |001.   Introduction.                                                                                                       #
#   |   |   |---------------------------------------------------------------------------------------------------------------------------#
#   |   |   |   |This method is intended to remove a signature from the internal registry of the hotkeys                                #
#   |   |   |---------------------------------------------------------------------------------------------------------------------------#
#   |   |   |100.   Parameters.                                                                                                         #
#   |   |   |---------------------------------------------------------------------------------------------------------------------------#
#   |   |   |key               :   <String   > String representation of the hotkey event, i.e. `event.key`                              #
#   |   |   |modSignature      :   <String   > The unique signature of the hotkey to identify from the internal registry                #
#   |   |   |registrationId    :   <String   > ID to match to ensure the removal                                                        #
#   |   |   |---------------------------------------------------------------------------------------------------------------------------#
#   |   |   |900.   Return Values by position.                                                                                          #
#   |   |   |---------------------------------------------------------------------------------------------------------------------------#
#   |   |   |<null>            :   This function only conducts the removal process                                                      #
#   |   |   |---------------------------------------------------------------------------------------------------------------------------#
#   |   |-------------------------------------------------------------------------------------------------------------------------------#
#   |   |[#unregisterById]                                                                                                              #
#   |   |-------------------------------------------------------------------------------------------------------------------------------#
#   |   |   |001.   Introduction.                                                                                                       #
#   |   |   |---------------------------------------------------------------------------------------------------------------------------#
#   |   |   |   |This method is intended to unregister a hotkey from the `component` by ID in the internal registry                     #
#   |   |   |---------------------------------------------------------------------------------------------------------------------------#
#   |   |   |100.   Parameters.                                                                                                         #
#   |   |   |---------------------------------------------------------------------------------------------------------------------------#
#   |   |   |component         :   <Object   > Web element for the hotkey to unbind, e.g. obtained by `QuerySelector()`                 #
#   |   |   |key               :   <String   > String representation of the hotkey event, i.e. `event.key`                              #
#   |   |   |modSignature      :   <String   > The unique signature of the hotkey to identify from the internal registry                #
#   |   |   |registrationId    :   <String   > ID to match to ensure the removal                                                        #
#   |   |   |---------------------------------------------------------------------------------------------------------------------------#
#   |   |   |900.   Return Values by position.                                                                                          #
#   |   |   |---------------------------------------------------------------------------------------------------------------------------#
#   |   |   |<null>            :   This function only conducts the removal process                                                      #
#   |   |   |---------------------------------------------------------------------------------------------------------------------------#
#   |   |-------------------------------------------------------------------------------------------------------------------------------#
#   |   |[#getTotalRegistrationCount]                                                                                                   #
#   |   |-------------------------------------------------------------------------------------------------------------------------------#
#   |   |   |001.   Introduction.                                                                                                       #
#   |   |   |---------------------------------------------------------------------------------------------------------------------------#
#   |   |   |   |This method is intended to obtain the number of registered hotkeys in current instance                                 #
#   |   |   |---------------------------------------------------------------------------------------------------------------------------#
#   |   |   |100.   Parameters.                                                                                                         #
#   |   |   |---------------------------------------------------------------------------------------------------------------------------#
#   |   |   |<null>            :   This function does not take parameters                                                               #
#   |   |   |---------------------------------------------------------------------------------------------------------------------------#
#   |   |   |900.   Return Values by position.                                                                                          #
#   |   |   |---------------------------------------------------------------------------------------------------------------------------#
#   |   |   |<Number>          :   Return the number of hotkeys that have been registered in current instance                           #
#   |   |   |---------------------------------------------------------------------------------------------------------------------------#
#   |   |-------------------------------------------------------------------------------------------------------------------------------#
#   |   |[#isEditableElement]                                                                                                           #
#   |   |-------------------------------------------------------------------------------------------------------------------------------#
#   |   |   |001.   Introduction.                                                                                                       #
#   |   |   |---------------------------------------------------------------------------------------------------------------------------#
#   |   |   |   |This method is intended to check whether the provided `element` is editable, e.g. `input box`                          #
#   |   |   |---------------------------------------------------------------------------------------------------------------------------#
#   |   |   |100.   Parameters.                                                                                                         #
#   |   |   |---------------------------------------------------------------------------------------------------------------------------#
#   |   |   |el                :   <Object   > Web element for the hotkey to unbind, e.g. obtained by `QuerySelector()`                 #
#   |   |   |---------------------------------------------------------------------------------------------------------------------------#
#   |   |   |900.   Return Values by position.                                                                                          #
#   |   |   |---------------------------------------------------------------------------------------------------------------------------#
#   |   |   |<Boolean>         :   `true` if the `el` is editable                                                                       #
#   |   |   |---------------------------------------------------------------------------------------------------------------------------#
#   |   |-------------------------------------------------------------------------------------------------------------------------------#
#   |   |[#handleKeyDown]                                                                                                               #
#   |   |-------------------------------------------------------------------------------------------------------------------------------#
#   |   |   |001.   Introduction.                                                                                                       #
#   |   |   |---------------------------------------------------------------------------------------------------------------------------#
#   |   |   |   |This method is intended to create the universal handler for the `keydown` event, to bind to the related component later#
#   |   |   |   |-----------------------------------------------------------------------------------------------------------------------#
#   |   |   |   |PROCESS                                                                                                                #
#   |   |   |   |-----------------------------------------------------------------------------------------------------------------------#
#   |   |   |   |[1] Ignores the pressing of the sole modifier keys                                                                     #
#   |   |   |   |[2] Prepare the map of all keys on the keyboard, and unique map of `numpad` keys to distinguish the registry           #
#   |   |   |   |[3] Identify all the keys pressed in current event and verify them in the maps                                         #
#   |   |   |   |[4] Prevent or allow repeated event (press and hold) by checking the `options` of the same hotkey in all registered    #
#   |   |   |   |    signatures                                                                                                         #
#   |   |   |   |[5] Prevent or allow triggering the `handler` when focusing an editable element by checking if at least one            #
#   |   |   |   |    registration allows such                                                                                           #
#   |   |   |   |[6] Execute the `handler` for all `component`s that are bound to current hotkey                                        #
#   |   |   |   |[7] `event.preventDefault()` if any one requires such; `event.stopPropagation()` if any one requires such              #
#   |   |   |---------------------------------------------------------------------------------------------------------------------------#
#   |   |   |100.   Parameters.                                                                                                         #
#   |   |   |---------------------------------------------------------------------------------------------------------------------------#
#   |   |   |event             :   <Object   > The keyboard event to trigger the internal handling                                      #
#   |   |   |---------------------------------------------------------------------------------------------------------------------------#
#   |   |   |900.   Return Values by position.                                                                                          #
#   |   |   |---------------------------------------------------------------------------------------------------------------------------#
#   |   |   |<Any>             :   The result returned from `handler` as the hotkey is registered for the dedicated `component`         #
#   |   |   |---------------------------------------------------------------------------------------------------------------------------#
#   |   |-------------------------------------------------------------------------------------------------------------------------------#
#   |   |[#startListening]                                                                                                              #
#   |   |-------------------------------------------------------------------------------------------------------------------------------#
#   |   |   |001.   Introduction.                                                                                                       #
#   |   |   |---------------------------------------------------------------------------------------------------------------------------#
#   |   |   |   |This method is intended to add a global listener to listen the `keydown` event for the `handler`s                      #
#   |   |   |---------------------------------------------------------------------------------------------------------------------------#
#   |   |   |100.   Parameters.                                                                                                         #
#   |   |   |---------------------------------------------------------------------------------------------------------------------------#
#   |   |   |<null>            :   This function does not take parameters                                                               #
#   |   |   |---------------------------------------------------------------------------------------------------------------------------#
#   |   |   |900.   Return Values by position.                                                                                          #
#   |   |   |---------------------------------------------------------------------------------------------------------------------------#
#   |   |   |<null>            :   This function only activates the global listening                                                    #
#   |   |   |---------------------------------------------------------------------------------------------------------------------------#
#   |   |-------------------------------------------------------------------------------------------------------------------------------#
#   |   |[#stopListening]                                                                                                               #
#   |   |-------------------------------------------------------------------------------------------------------------------------------#
#   |   |   |001.   Introduction.                                                                                                       #
#   |   |   |---------------------------------------------------------------------------------------------------------------------------#
#   |   |   |   |This method is intended to remove the global listener to listen the `keydown` event for the `handler`s                 #
#   |   |   |---------------------------------------------------------------------------------------------------------------------------#
#   |   |   |100.   Parameters.                                                                                                         #
#   |   |   |---------------------------------------------------------------------------------------------------------------------------#
#   |   |   |<null>            :   This function does not take parameters                                                               #
#   |   |   |---------------------------------------------------------------------------------------------------------------------------#
#   |   |   |900.   Return Values by position.                                                                                          #
#   |   |   |---------------------------------------------------------------------------------------------------------------------------#
#   |   |   |<null>            :   This function only removes the global event listener                                                 #
#   |   |   |---------------------------------------------------------------------------------------------------------------------------#
#   |   |-------------------------------------------------------------------------------------------------------------------------------#
#   |-----------------------------------------------------------------------------------------------------------------------------------#
#---------------------------------------------------------------------------------------------------------------------------------------#
#300.   Update log.                                                                                                                     #
#---------------------------------------------------------------------------------------------------------------------------------------#
#   | Date |    20260622        | Version | 1.00        | Updater/Creator | Lu Robin Bin                                                #
#   |______|____________________|_________|_____________|_________________|_____________________________________________________________#
#   | Log  |Version 1.                                                                                                                  #
#   |______|____________________________________________________________________________________________________________________________#
#---------------------------------------------------------------------------------------------------------------------------------------#
#400.   User Manual.                                                                                                                    #
#---------------------------------------------------------------------------------------------------------------------------------------#
#   |See the [Full Test Program] section                                                                                                #
#---------------------------------------------------------------------------------------------------------------------------------------#
#500.   Dependent Facilities.                                                                                                           #
#---------------------------------------------------------------------------------------------------------------------------------------#
#   |100.   Dependent Modules                                                                                                           #
#   |-----------------------------------------------------------------------------------------------------------------------------------#
#   |inspect                                                                                                                            #
#   |-----------------------------------------------------------------------------------------------------------------------------------#
#   |300.   Dependent user-defined functions                                                                                            #
#   |-----------------------------------------------------------------------------------------------------------------------------------#
#---------------------------------------------------------------------------------------------------------------------------------------#
    '''

    #050. Local parameters

    #100. Setup the JS program
    js_snippet = cleandoc(f'''
        // ==================== HotkeyManager 核心类 v3 (小键盘支持) ====================

        /**
         * 全局快捷键管理器
         *
         * 功能：
         * - 解析 'ctrl+alt+f' 或 'shift+ArrowUp' 等格式的快捷键字符串
         * - 字母/数字/功能键/方向键/特殊键等任意单一按键（除功能修饰键外）
         * - 全局监听 keydown 事件并匹配快捷键
         * - 动态注册/注销组件对应的 handler
         * - 支持 Windows 和 macOS 的功能键
         * - 自动跳过可编辑元素（input/textarea/select/contenteditable）
         * - 支持暂停/恢复
         * - 冲突检测与警告
         *
         * 功能键：
         *   ctrl    → Ctrl 键
         *   alt     → Alt 键 (Mac Option)
         *   shift   → Shift 键
         *   command → Command, cmd 键 (Mac ⌘) / Windows 键 (Win ⊞)
         *   option  → Option, opt 键 (Mac ⌥) / Alt 键 (与 alt 统一为 altKey)
         *
         * 目标按键支持：
         *   - 字母 a-z (大小写不敏感)
         *   - 数字 0-9
         *   - 功能键 F1-F12
         *   - 方向键 up/down/left/right 或 ArrowUp 等
         *   - 特殊键 space/enter/escape/backspace/tab/delete 等
         *   - 标点符号等单字符键
         *   - 数字小键盘的按键
         *
         * 快捷键格式示例：
         *   'ctrl+alt+f'
         *   'command+shift+arrowup'
         *   'ctrl+f5'
         *   'shift+space'
         */
        class {funcName} {{
        	#options;
        	#KEY_ALIASES;
        	#registry;
        	#componentIndex;
        	#idCounter;
        	#paused;
        	#listening;

            /**
             * @param {{Object}} [options] - 全局配置
             * @param {{boolean}} [options.ignoreEditable=true] - 是否在可编辑元素中获得焦点时跳过快捷键
             * @param {{boolean}} [options.preventDefault=true] - 匹配成功后是否阻止浏览器默认行为
             * @param {{boolean}} [options.stopPropagation=true] - 匹配成功后是否阻止事件冒泡
             * @param {{boolean}} [options.ignoreRepeat=true] - 默认忽略repeat事件
             * @param {{boolean}} [options.debug=false] - 是否输出调试日志
             */
            constructor(options = {{}}) {{
                this.#options = {{
                    ignoreEditable: options.ignoreEditable !== false,
                    preventDefault: options.preventDefault !== false,
                    stopPropagation: options.stopPropagation !== false,
                    ignoreRepeat: options.ignoreRepeat !== false,
                    debug: options.debug === true,
                }};

                // 数据结构：
                // #registry: Map<letter, Map<modifierSignature, Set<Registration>>>
                //   letter: 单个小写字母，如 'f'
                //   modifierSignature: 规范化后的功能键签名，如 'alt+ctrl'（已排序）
                //   Registration: {{ component, handler, hotkeyString, options }}
                // 别名表：用户输入 -> 标准内部键名 (基于 event.code 或 event.key)
                this.#KEY_ALIASES = {{
                    // 字母数字符号
                    'up': 'ArrowUp',
                    'down': 'ArrowDown',
                    'left': 'ArrowLeft',
                    'right': 'ArrowRight',
                    'arrowup': 'ArrowUp',
                    'arrowdown': 'ArrowDown',
                    'arrowleft': 'ArrowLeft',
                    'arrowright': 'ArrowRight',
                    'space': ' ',
                    'spacebar': ' ',
                    'enter': 'Enter',
                    'return': 'Enter',
                    'escape': 'Escape',
                    'esc': 'Escape',
                    'backspace': 'Backspace',
                    'tab': 'Tab',
                    'delete': 'Delete',
                    'del': 'Delete',
                    'insert': 'Insert',
                    'ins': 'Insert',
                    'home': 'Home',
                    'end': 'End',
                    'pageup': 'PageUp',
                    'pagedown': 'PageDown',
                    'capslock': 'CapsLock',
                    'numlock': 'NumLock',
                    'scrolllock': 'ScrollLock',
                    'f1': 'F1',
                    'f2': 'F2',
                    'f3': 'F3',
                    'f4': 'F4',
                    'f5': 'F5',
                    'f6': 'F6',
                    'f7': 'F7',
                    'f8': 'F8',
                    'f9': 'F9',
                    'f10': 'F10',
                    'f11': 'F11',
                    'f12': 'F12',
                    'printscreen': 'PrintScreen',
                    'prtsc': 'PrintScreen',
                    'pause': 'Pause',

                    // 小键盘物理按键映射 -> event.code (不受NumLock影响)
                    'numpad0': 'Numpad0',
                    'numpad1': 'Numpad1',
                    'numpad2': 'Numpad2',
                    'numpad3': 'Numpad3',
                    'numpad4': 'Numpad4',
                    'numpad5': 'Numpad5',
                    'numpad6': 'Numpad6',
                    'numpad7': 'Numpad7',
                    'numpad8': 'Numpad8',
                    'numpad9': 'Numpad9',
                    'numpad.': 'NumpadDecimal',
                    'numpaddecimal': 'NumpadDecimal',
                    'numpad/': 'NumpadDivide',
                    'numpaddivide': 'NumpadDivide',
                    'numpad*': 'NumpadMultiply',
                    'numpadmultiply': 'NumpadMultiply',
                    'numpad-': 'NumpadSubtract',
                    'numpadsubtract': 'NumpadSubtract',
                    'numpad+': 'NumpadAdd',
                    'numpadadd': 'NumpadAdd',
                    'numpadenter': 'NumpadEnter',
                }};

                // 主索引：Map<standardKey, Map<modifierSignature, Set<Registration>>>
                this.#registry = new Map();
                // 反向索引：component -> Set<{{ key, modSignature, hotkeyString, registrationId }}>
                // 用于快速注销某个组件的所有快捷键
                this.#componentIndex = new Map();

                // 全局计数器，用于生成唯一ID
                this.#idCounter = 0;

                // 是否暂停
                this.#paused = false;

                // 绑定的keydown监听器引用
                /**
                    [ASSUMPTION]
                    [1] 经测试，这里需要绑定 this 的方法均在 AddEveneListener 中绑定给外部事件
                    [2] 因此，这些方法不能是私有方法，需要暴露给外部环境
                    [3] 用下划线开头是伪装成私有的公有方法，因此可以使用
                    [4] 这里公开一个方法 `_parseHotkeyString` 供外部模拟手动注册新快捷键时的校验
                 */
                this._parseHotkeyString = this.#parseHotkeyString.bind(this);
                this._boundKeydownHandler = null;
                this.#listening = false;

                // 自动开始监听
                this.#startListening();

                if (this.#options.debug) {{
                    console.log('[{funcName}] v3.4 初始化完成 (已启用防重复触发)');
                }}
            }}

            // -------------------- 公开API --------------------

            /**
             * 注册快捷键
             *
             * @param {{*}} component - 组件标识符（用于注销时识别），通常是组件实例或DOM元素
             * @param {{string}} hotkeyString - 快捷键字符串，如 'ctrl+alt+f'
             * @param {{Function}} handler - 回调函数，接收 KeyboardEvent
             * @param {{Object}} [options] - 该快捷键的独立配置（会与全局配置合并）
             * @param {{boolean}} [options.ignoreEditable] - 覆盖全局的 ignoreEditable 设置
             * @param {{boolean}} [options.preventDefault] - 覆盖全局的 preventDefault 设置
             * @param {{boolean}} [options.stopPropagation] - 覆盖全局的 stopPropagation 设置
             * @param {{boolean}} [options.ignoreRepeat] - 覆盖全局的 ignoreRepeat 设置
             * @param {{string}} [options.description] - 快捷键描述（用于调试和展示）
             * @returns {{Function}} 返回一个取消注册的函数，调用即可注销此快捷键
             * @throws {{Error}} 如果快捷键格式不合法
             */
            register(component, hotkeyString, handler, options = {{}}) {{
                // 解析并验证快捷键字符串
                const parsed = this.#parseHotkeyString(hotkeyString);
                const {{ standardKey, modifierSet, normalizedSignature }} = parsed;

                // 验证handler
                if (typeof handler !== 'function') {{
                    throw new Error('[{funcName}] handler 必须是一个函数');
                }}

                // 创建注册记录
                const registration = {{
                    id: ++this.#idCounter,
                    component,
                    handler,
                    hotkeyString: (normalizedSignature ? normalizedSignature + '+' : '') + standardKey,
                    originalHotkeyString: hotkeyString.trim().toLowerCase(),
                    options: {{
                        ignoreEditable: options.ignoreEditable !== undefined ?
                            options.ignoreEditable : this.#options.ignoreEditable,
                        preventDefault: options.preventDefault !== undefined ?
                            options.preventDefault : this.#options.preventDefault,
                        stopPropagation: options.stopPropagation !== undefined ?
                            options.stopPropagation : this.#options.stopPropagation,
                        ignoreRepeat: options.ignoreRepeat !== undefined ?
                            options.ignoreRepeat : this.#options.ignoreRepeat,
                    }},
                    description: options.description || null,
                    registeredAt: new Date(),
                }};

                // 存入主索引
                if (!this.#registry.has(standardKey)) {{
                    this.#registry.set(standardKey, new Map());
                }}
                const keyMap = this.#registry.get(standardKey);
                if (!keyMap.has(normalizedSignature)) {{
                    keyMap.set(normalizedSignature, new Set());
                }}
                const regSet = keyMap.get(normalizedSignature);

                // 冲突检测：同一个快捷键已存在其他注册
                if (regSet.size > 0 && this.#options.debug) {{
                    console.warn(
                        `[{funcName}] ⚠️ 快捷键 '${{registration.hotkeyString}}' 已有 ${{regSet.size}} 个注册，新handler将一并执行。`,
                        '已有:', Array.from(regSet).map(r => r.description || r.originalHotkeyString).join(', ')
                    );
                }}
                regSet.add(registration);

                // 存入反向索引
                if (!this.#componentIndex.has(component)) {{
                    this.#componentIndex.set(component, new Set());
                }}
                this.#componentIndex.get(component).add({{
                    key: standardKey,
                    modSignature: normalizedSignature,
                    hotkeyString: registration.hotkeyString,
                    registrationId: registration.id,
                }});

                if (this.#options.debug) {{
                    console.log(
                        `[{funcName}] ✅ 注册成功: '${{registration.hotkeyString}}'`,
                        registration.description ? `(${{registration.description}})` : '',
                        `| 当前该快捷键共 ${{regSet.size}} 个handler`
                    );
                }}

                // 返回取消注册函数
                return () => this.#unregisterById(component, standardKey, normalizedSignature, registration.id);
            }}

            /**
             * 注销指定组件的快捷键
             *
             * @param {{*}} component - 组件标识符
             * @param {{string}} [hotkeyString] - 可选，指定要注销的快捷键字符串；不传则注销该组件的所有快捷键
             * @returns {{number}} 返回实际注销的快捷键数量
             */
            unregister(component, hotkeyString) {{
                if (!this.#componentIndex.has(component)) return 0;
                const indexEntries = this.#componentIndex.get(component);
                let removedCount = 0;

                if (hotkeyString) {{
                    // 注销指定快捷键
                    const parsed = this.#safeParse(hotkeyString);
                    if (!parsed) return 0;
                    const toRemove = [];
                    for (const entry of indexEntries) {{
                        if (entry.key === parsed.standardKey && entry.modSignature === parsed.normalizedSignature) {{
                            toRemove.push(entry);
                        }}
                    }}
                    for (const entry of toRemove) {{
                        this.#removeFromRegistry(entry.key, entry.modSignature, entry.registrationId);
                        indexEntries.delete(entry);
                        removedCount++;
                    }}
                }} else {{
                    // 注销该组件的所有快捷键
                    for (const entry of indexEntries) {{
                        this.#removeFromRegistry(entry.key, entry.modSignature, entry.registrationId);
                        removedCount++;
                    }}
                    indexEntries.clear();
                    this.#componentIndex.delete(component);
                }}

                // 清理空索引
                if (indexEntries && indexEntries.size === 0) {{
                    this.#componentIndex.delete(component);
                }}

                if (this.#options.debug && removedCount > 0) {{
                    console.log(`[{funcName}] 🗑️ 已注销 ${{removedCount}} 个快捷键`);
                }}
                return removedCount;
            }}

            /**
             * 注销所有快捷键
             */
            unregisterAll() {{
                const count = this.#getTotalRegistrationCount();
                this.#registry.clear();
                this.#componentIndex.clear();
                if (this.#options.debug) {{
                    console.log(`[{funcName}] 🗑️ 已清除全部 ${{count}} 个快捷键注册`);
                }}
                return count;
            }}

            /**
             * 暂停快捷键响应（在模态框、对话框打开时使用）
             */
            pause() {{
                this.#paused = true;
                if (this.#options.debug) {{
                    console.log('[{funcName}] ⏸️ 已暂停');
                }}
            }}

            /**
             * 恢复快捷键响应
             */
            resume() {{
                this.#paused = false;
                if (this.#options.debug) {{
                    console.log('[{funcName}] ▶️ 已恢复');
                }}
            }}

            /**
             * 是否处于暂停状态
             * @returns {{boolean}}
             */
            isPaused() {{
                return this.#paused;
            }}

            /**
             * 检查快捷键是否已被注册
             * @param {{string}} hotkeyString
             * @returns {{boolean}}
             */
            isRegistered(hotkeyString) {{
                const parsed = this.#safeParse(hotkeyString);
                if (!parsed) return false;
                const keyMap = this.#registry.get(parsed.standardKey);
                if (!keyMap) return false;
                const regSet = keyMap.get(parsed.normalizedSignature);
                return regSet ? regSet.size > 0 : false;
            }}

            /**
             * 获取所有已注册的快捷键信息
             * @returns {{Array<{{hotkeyString: string, count: number, descriptions: string[]}}>}}
             */
            getRegisteredHotkeys() {{
                const result = [];
                for (const [key, keyMap] of this.#registry) {{
                    for (const [modSig, regSet] of keyMap) {{
                        if (regSet.size > 0) {{
                            const hotkeyStr = modSig ? modSig + '+' + key : key;
                            const descriptions = [];
                            for (const reg of regSet) if (reg.description) descriptions.push(reg.description);
                            result.push({{
                                hotkeyString: hotkeyStr,
                                count: regSet.size,
                                descriptions: descriptions.length > 0 ? descriptions : null,
                            }});
                        }}
                    }}
                }}
                // 排序：按字母排序，再按功能键数量排序
                result.sort((a, b) => {{
                    const aKey = a.hotkeyString.split('+').pop();
                    const bKey = b.hotkeyString.split('+').pop();
                    if (aKey !== bKey) return aKey.localeCompare(bKey);
                    return a.hotkeyString.split('+').length - b.hotkeyString.split('+').length;
                }});
                return result;
            }}

            /**
             * 获取总注册数量
             * @returns {{number}}
             */
            getRegistrationCount() {{ return this.#getTotalRegistrationCount(); }}

            /**
             * 销毁管理器，移除所有监听器和注册
             */
            dispose() {{
                this.#stopListening();
                this.unregisterAll();
                if (this.#options.debug) {{
                    console.log('[{funcName}] 💥 已销毁');
                }}
            }}

            // -------------------- 私有方法 --------------------

            // --- 标准化键名 (支持小键盘) ---
            /**
             * 标准化按键名称
             * @param {{string}} rawKey - 快捷键字符串中的键部分或 event.key
             * @returns {{string}} 标准键名 (如 'a', 'ArrowUp', 'F1', ' ')
             */
            #normalizeKey(rawKey) {{
                const lower = rawKey.toLowerCase();
                if (this.#KEY_ALIASES.hasOwnProperty(lower)) {{
                    return this.#KEY_ALIASES[lower];
                }}
                // 单字符：字母、数字、符号 -> 小写
                if (rawKey.length === 1) return lower;
                // 其他情况直接返回（可能已经是标准键名，如 'ArrowUp'）
                return rawKey;
            }}

            // ---------- 新解析方法：不依赖 split('+') ----------
            /**
             * 解析快捷键字符串
             * @param {{string}} hotkeyString - 如 'ctrl+alt+f'
             * @returns {{{{ standardKey: string, modifierSet: Set<string>, normalizedSignature: string }}}}
             * @throws {{Error}} 格式不合法时抛出
             */
            #parseHotkeyString(hotkeyString) {{
                const trimmed = hotkeyString.trim().toLowerCase();
                if (!trimmed) throw new Error('[{funcName}] 快捷键字符串不能为空');

                // 允许的修饰键集合
                const MODIFIERS = ['ctrl', 'alt', 'shift', 'command', 'cmd', 'option', 'opt', 'meta'];
                const modifierSet = new Set();
                let remaining = trimmed;

                // 循环匹配修饰键，修饰键后必须紧跟 '+' 或到达字符串末尾
                let matched;
                do {{
                    matched = false;
                    for (const mod of MODIFIERS) {{
                        // 检查剩余字符串是否以 'mod+' 或 'mod' 开头且后面是结尾
                        if (remaining.startsWith(mod + '+')) {{
                            // 是修饰键，加入集合
                            if (mod === 'option' || mod === 'opt') {{
                                modifierSet.add('alt');
                            }} else if (mod === 'command' || mod === 'cmd' || mod === 'meta') {{
                                modifierSet.add('meta');
                            }} else {{
                                modifierSet.add(mod);
                            }}
                            remaining = remaining.slice(mod.length + 1); // 移除 'mod+'
                            matched = true;
                            break;
                        }} else if (remaining === mod) {{
                            // 修饰键单独出现（但后面没有目标键，不应该出现，抛出错误）
                            throw new Error(`[{funcName}] 快捷键缺少目标按键: '${{hotkeyString}}'`);
                        }}
                    }}
                }} while (matched);

                if (!remaining) throw new Error(`[{funcName}] 缺少目标按键: '${{hotkeyString}}'`);
                // 剩余部分就是目标键（可能包含 '+' 等符号）
                const rawKey = remaining;

                // 目标键不能是修饰键本身 (以防有人写 'ctrl+ctrl')
                if (MODIFIERS.includes(rawKey)) throw new Error(`[{funcName}] 目标按键不能是功能键: '${{rawKey}}'`);

                // 生成规范化签名（排序后的功能键用'+'连接）
                const standardKey = this.#normalizeKey(rawKey);
                const sortedMods = [...modifierSet].sort();
                return {{ standardKey, modifierSet, normalizedSignature: sortedMods.join('+') }};
            }}

            /**
             * 安全解析，不抛出异常
             */
            #safeParse(hotkeyString) {{
                try {{
                    return this.#parseHotkeyString(hotkeyString);
                }} catch {{
                    return null;
                }}
            }}

            /**
             * 从主索引中移除一条注册记录
             */
            #removeFromRegistry(key, modSignature, registrationId) {{
                const keyMap = this.#registry.get(key);
                if (!keyMap) return;
                const regSet = keyMap.get(modSignature);
                if (!regSet) return;
                for (const reg of regSet) {{
                    if (reg.id === registrationId) {{
                        regSet.delete(reg);
                        break;
                    }}
                }}
                // 清理空容器
                if (regSet.size === 0) keyMap.delete(modSignature);
                if (keyMap.size === 0) this.#registry.delete(key);
            }}

            /**
             * 通过ID注销
             */
            #unregisterById(component, key, modSignature, registrationId) {{
                this.#removeFromRegistry(key, modSignature, registrationId);

                const modMap = this.#registry.get(key);
                if (modMap) {{
                    const set = modMap.get(modSignature);
                    if (set) for (const r of set) if (r.id === id) {{ set.delete(r); break; }}
                    if (set && set.size === 0) modMap.delete(modSignature);
                    if (modMap.size === 0) this.#registry.delete(key);
                }}

                // 更新反向索引
                const indexEntries = this.#componentIndex.get(component);
                if (indexEntries) {{
                    for (const entry of indexEntries) {{
                        if (entry.registrationId === registrationId) {{
                            indexEntries.delete(entry);
                            break;
                        }}
                    }}
                    if (indexEntries.size === 0) this.#componentIndex.delete(component);
                }}

                if (this.#options.debug) {{
                    console.log(`[{funcName}] 🗑️ 已注销快捷键 (ID: ${{registrationId}})`);
                }}
            }}

            /**
             * 获取总注册数
             */
            #getTotalRegistrationCount() {{
                let count = 0;
                for (const [, keyMap] of this.#registry) {{
                    for (const [, regSet] of keyMap) count += regSet.size;
                }}
                return count;
            }}

            /**
             * 判断元素是否为可编辑元素
             */
            #isEditableElement(el) {{
                if (!el || !el.tagName) return false;
                const tag = el.tagName.toLowerCase();
                if (tag === 'input' || tag === 'textarea' || tag === 'select') return true;
                if (el.isContentEditable) return true;
                if (el.getAttribute && el.getAttribute('contenteditable') === 'true') return true;
                // 检查角色
                if (el.getAttribute && ['textbox', 'searchbox', 'combobox'].includes(el.getAttribute('role') || '')) return true;
                return false;
            }}

            /**
             * 全局keydown事件处理 (v3.4 核心改进：检查 event.repeat)
             */
            #handleKeyDown(event) {{
                if (this.#paused) return;

                const rawEventKey = event.key;
                // 忽略功能键本身
                if (['Control', 'Alt', 'Shift', 'Meta'].includes(rawEventKey)) return;

                // 构建候选键列表：基于 event.key 和 event.code (小键盘)
                const candidateKeys = [];
                if (event.code) {{
                    candidateKeys.push(event.code);
                    const normalizedFromCode = this.#normalizeKey(event.code.toLowerCase());
                    if (normalizedFromCode !== event.code) candidateKeys.push(normalizedFromCode);
                }}
                const normalizedKey = this.#normalizeKey(rawEventKey);
                candidateKeys.push(normalizedKey);

                // 构建当前按下的修饰键集合
                const currentMods = new Set();
                if (event.ctrlKey) currentMods.add('ctrl');
                if (event.altKey) currentMods.add('alt');
                if (event.shiftKey) currentMods.add('shift');
                if (event.metaKey) currentMods.add('meta');

                // 生成当前修饰键签名
                const currentModSignature = Array.from(currentMods).sort().join('+');

                // 尝试每个候选键查找注册
                let matchingRegs = [];
                for (const candKey of candidateKeys) {{
                    const keyMap = this.#registry.get(candKey);
                    if (keyMap) {{
                        const regSet = keyMap.get(currentModSignature);
                        if (regSet && regSet.size > 0) {{
                            matchingRegs.push(...Array.from(regSet));
                        }}
                    }}
                }}
                if (matchingRegs.length === 0) return;

                // 去重 (可能同一注册被重复匹配)
                const uniqueRegs = [];
                const seen = new Set();
                for (const reg of matchingRegs) {{
                    if (!seen.has(reg.id)) {{
                        seen.add(reg.id);
                        uniqueRegs.push(reg);
                    }}
                }}

                // ✅ 检查所有handler的ignoreRepeat设置
                // 如果是重复事件，且所有handler都设置了ignoreRepeat=true，则跳过
                if (event.repeat) {{
                    let allIgnoreRepeat = true;
                    for (const reg of uniqueRegs) {{
                        if (reg.options.ignoreRepeat === false) {{
                            allIgnoreRepeat = false;
                            break;
                        }}
                    }}
                    if (allIgnoreRepeat) {{
                        if (this.#options.debug) {{
                            console.log(`[{funcName}] ⏭️ 忽略重复事件: '${{uniqueRegs[0].hotkeyString}}' (repeat=true)`);
                        }}
                        // 触发外部回调通知（可用于UI反馈）
                        if (typeof window._onHotkeyRepeatBlocked === 'function') {{
                            window._onHotkeyRepeatBlocked(uniqueRegs[0].hotkeyString);
                        }}
                        return;
                    }}
                }}

                // 如果至少有一个注册明确设置了ignoreEditable=false，则允许在可编辑元素中触发
                let hasExplicitAllow = false;
                for (const reg of uniqueRegs) {{
                    if (reg.options.ignoreEditable === false) {{
                        hasExplicitAllow = true;
                        break;
                    }}
                }}
                const shouldIgnore = !hasExplicitAllow;
                const target = event.target;

                if (shouldIgnore && this.#isEditableElement(target)) {{
                    if (this.#options.debug) {{
                        console.log(
                            `[{funcName}] ⏭️ 跳过可编辑元素中的快捷键: '${{uniqueRegs[0].hotkeyString}}'`
                        );
                    }}
                    return;
                }}

                // 执行所有匹配的handler
                let anyPreventDefault = false;
                let anyStopPropagation = false;

                for (const reg of uniqueRegs) {{
                    // 每个handler独立的选项
                    const opt = reg.options;
                    if (shouldIgnore && opt.ignoreEditable !== false && this.#isEditableElement(target)) continue;
                    if (opt.preventDefault) anyPreventDefault = true;
                    if (opt.stopPropagation) anyStopPropagation = true;
                    try {{
                        reg.handler(event);
                    }} catch (err) {{
                        console.error(`[{funcName}] ❌ handler执行错误 (${{reg.hotkeyString}}):`, err);
                    }}
                }}

                // 应用事件控制
                if (anyPreventDefault) event.preventDefault();
                if (anyStopPropagation) event.stopPropagation();
            }}

            /**
             * 开始全局监听
             */
            #startListening() {{
                if (this.#listening) return;
                this._boundKeydownHandler = this.#handleKeyDown.bind(this);
                document.addEventListener('keydown', this._boundKeydownHandler, true); // 使用捕获阶段以提高优先级
                this.#listening = true;
            }}

            /**
             * 停止全局监听
             */
            #stopListening() {{
                if (!this.#listening || !this._boundKeydownHandler) return;
                document.removeEventListener('keydown', this._boundKeydownHandler, true);
                this._boundKeydownHandler = null;
                this.#listening = false;
            }}
        }}
    ''')

    #900. Return the flag
    return(js_snippet)
#End jsHotkeyManager

'''
#-Notes- -Begin-
#Full Test Program[1]:
if __name__=='__main__':
    #010.   Create envionment.
    import os, re
    import sys
    import shutil
    import textwrap
    from inspect import cleandoc
    dir_omniPy : str = r'D:\Python\ '.strip()
    if dir_omniPy not in sys.path:
        sys.path.append( dir_omniPy )

    from omniPy.ShinyApp import jsHotkeyManager
    print(jsHotkeyManager.__doc__)

    #100. View the JS as a character string
    hkManager = jsHotkeyManager()
    # print(hkManager)

    #300. Test the functionality
    #[ASSUMPTION]
    #[1] App中若有大量操作，可适当增添快捷键；用这个类可以只创建一个全局监听
    # see <ShinyApp.Modules.OSNativeFileSelector> for sample usage

    #301. Locate the dedicated App
    dst_dir = r'D:\Temp\test_html'
    dst_html = os.path.join(dst_dir, 'jsHotkeyManager.html')

    if not os.path.isdir(dst_dir): os.makedirs(dst_dir)

    #370. Prepare the full test program
    hkManager_str = textwrap.indent(hkManager, ' ' * 16).strip()

    html_snippet = cleandoc(f"""
        <!DOCTYPE html>
        <html lang="zh-CN">
        <head>
            <meta charset="UTF-8">
            <meta name="viewport" content="width=device-width, initial-scale=1.0">
            <title>HotkeyManager v3.4 · 防重复触发 · 忽略repeat事件</title>
            <style>
                :root {{
                    --bg: #f5f6fa;
                    --card-bg: #ffffff;
                    --text: #2d3436;
                    --text-secondary: #636e72;
                    --border: #e0e4e8;
                    --shadow: 0 2px 12px rgba(0, 0, 0, 0.06);
                    --shadow-hover: 0 8px 30px rgba(0, 0, 0, 0.10);
                    --accent: #5b6af0;
                    --accent-hover: #4a56d4;
                    --accent-light: #eef0ff;
                    --success: #00b894;
                    --danger: #e17055;
                    --warning: #fdcb6e;
                    --radius: 14px;
                    --radius-sm: 8px;
                    --transition: 0.25s cubic-bezier(0.4, 0, 0.2, 1);
                    --font-mono: 'SF Mono', 'Cascadia Code', 'JetBrains Mono', 'Fira Code', 'Consolas', monospace;
                    --font-sans: 'Inter', 'Segoe UI', 'PingFang SC', 'Microsoft YaHei', sans-serif;
                }}

                * {{
                    margin: 0;
                    padding: 0;
                    box-sizing: border-box;
                }}

                body {{
                    font-family: var(--font-sans);
                    background: var(--bg);
                    color: var(--text);
                    min-height: 100vh;
                    display: flex;
                    justify-content: center;
                    align-items: flex-start;
                    padding: 40px 20px 60px;
                    -webkit-font-smoothing: antialiased;
                }}

                .container {{
                    width: 100%;
                    max-width: 820px;
                    display: flex;
                    flex-direction: column;
                    gap: 28px;
                }}

                .header {{
                    text-align: center;
                    padding: 10px 0 4px;
                }}
                .header .icon-wrapper {{
                    display: inline-flex;
                    align-items: center;
                    justify-content: center;
                    width: 56px;
                    height: 56px;
                    border-radius: 16px;
                    background: var(--accent-light);
                    margin-bottom: 12px;
                    font-size: 26px;
                }}
                .header h1 {{
                    font-size: 1.8rem;
                    font-weight: 700;
                    letter-spacing: -0.4px;
                    color: #1a1a2e;
                    margin-bottom: 4px;
                }}
                .header .subtitle {{
                    font-size: 0.9rem;
                    color: var(--text-secondary);
                    font-weight: 400;
                }}
                .platform-badge {{
                    display: inline-block;
                    padding: 4px 12px;
                    border-radius: 20px;
                    font-size: 0.78rem;
                    font-weight: 500;
                    background: #f0f0f5;
                    color: #555;
                    margin-top: 6px;
                    letter-spacing: 0.2px;
                }}
                .platform-badge.mac {{
                    background: #e8f0fe;
                    color: #3b5998;
                }}
                .platform-badge.win {{
                    background: #eef6f0;
                    color: #3d6b4f;
                }}

                .card {{
                    background: var(--card-bg);
                    border-radius: var(--radius);
                    padding: 26px 30px;
                    box-shadow: var(--shadow);
                    border: 1px solid var(--border);
                    transition: var(--transition);
                    position: relative;
                    overflow: hidden;
                }}
                .card:hover {{
                    box-shadow: var(--shadow-hover);
                }}
                .card.flash {{
                    animation: cardFlash 0.5s ease-out;
                }}
                @keyframes cardFlash {{
                    0% {{
                        box-shadow: 0 0 0 0 rgba(91, 106, 240, 0.55);
                        border-color: var(--accent);
                    }}
                    100% {{
                        box-shadow: var(--shadow);
                        border-color: var(--border);
                    }}
                }}
                .card-header {{
                    display: flex;
                    align-items: center;
                    gap: 12px;
                    margin-bottom: 18px;
                }}
                .card-icon {{
                    width: 40px;
                    height: 40px;
                    border-radius: 10px;
                    display: flex;
                    align-items: center;
                    justify-content: center;
                    font-size: 18px;
                    flex-shrink: 0;
                }}
                .card-icon.btn-icon {{
                    background: #fff0e8;
                }}
                .card-icon.switch-icon {{
                    background: #e8faf3;
                }}
                .card-icon.custom-icon {{
                    background: #f3e8ff;
                }}
                .card-icon.numpad-icon {{
                    background: #e8f0fe;
                }}
                .card-title {{
                    font-weight: 600;
                    font-size: 1.05rem;
                    letter-spacing: -0.2px;
                }}
                .card-badge {{
                    display: inline-flex;
                    align-items: center;
                    gap: 4px;
                    padding: 5px 10px;
                    border-radius: 6px;
                    font-family: var(--font-mono);
                    font-size: 0.8rem;
                    font-weight: 500;
                    background: #f8f9fc;
                    border: 1px solid #e8ecf2;
                    letter-spacing: 0.3px;
                    white-space: nowrap;
                }}
                .card-badge .key-symbol {{
                    color: var(--accent);
                    font-weight: 700;
                }}

                .demo-btn {{
                    display: inline-flex;
                    align-items: center;
                    gap: 8px;
                    padding: 11px 22px;
                    border-radius: 8px;
                    font-size: 0.95rem;
                    font-weight: 600;
                    cursor: pointer;
                    border: none;
                    background: var(--accent);
                    color: #fff;
                    transition: var(--transition);
                    letter-spacing: 0.1px;
                    user-select: none;
                    -webkit-tap-highlight-color: transparent;
                    position: relative;
                    overflow: hidden;
                }}
                .demo-btn:hover {{
                    background: var(--accent-hover);
                    transform: translateY(-1px);
                    box-shadow: 0 6px 20px rgba(91, 106, 240, 0.35);
                }}
                .demo-btn:active {{
                    transform: scale(0.96);
                    transition: 0.1s;
                }}
                .demo-btn.triggered {{
                    animation: btnPulse 0.45s ease-out;
                }}
                @keyframes btnPulse {{
                    0% {{
                        box-shadow: 0 0 0 0 rgba(91, 106, 240, 0.7);
                    }}
                    50% {{
                        box-shadow: 0 0 0 18px rgba(91, 106, 240, 0);
                    }}
                    100% {{
                        box-shadow: 0 0 0 0 rgba(91, 106, 240, 0);
                    }}
                }}
                .click-count {{
                    display: inline-block;
                    padding: 4px 12px;
                    border-radius: 14px;
                    background: #f0f0f8;
                    font-weight: 600;
                    font-size: 0.85rem;
                    color: var(--accent);
                    min-width: 40px;
                    text-align: center;
                    transition: var(--transition);
                }}
                .click-count.bump {{
                    animation: countBump 0.35s ease-out;
                }}
                @keyframes countBump {{
                    0% {{
                        transform: scale(1);
                    }}
                    40% {{
                        transform: scale(1.35);
                        color: #e17055;
                    }}
                    100% {{
                        transform: scale(1);
                    }}
                }}

                .switch-row {{
                    display: flex;
                    align-items: center;
                    gap: 16px;
                    flex-wrap: wrap;
                }}
                .switch-wrapper {{
                    position: relative;
                    width: 56px;
                    height: 30px;
                    cursor: pointer;
                    -webkit-tap-highlight-color: transparent;
                    flex-shrink: 0;
                }}
                .switch-track {{
                    position: absolute;
                    inset: 0;
                    border-radius: 15px;
                    background: #d5d8df;
                    transition: var(--transition);
                    cursor: pointer;
                }}
                .switch-wrapper.on .switch-track {{
                    background: var(--success);
                }}
                .switch-thumb {{
                    position: absolute;
                    top: 3px;
                    left: 3px;
                    width: 24px;
                    height: 24px;
                    border-radius: 50%;
                    background: #fff;
                    box-shadow: 0 2px 6px rgba(0, 0, 0, 0.18);
                    transition: var(--transition);
                    cursor: pointer;
                }}
                .switch-wrapper.on .switch-thumb {{
                    left: 29px;
                }}
                .switch-wrapper:active .switch-thumb {{
                    width: 28px;
                    border-radius: 14px;
                    left: 1px;
                }}
                .switch-wrapper.on:active .switch-thumb {{
                    left: 27px;
                }}
                .switch-label {{
                    font-weight: 500;
                    font-size: 0.95rem;
                    user-select: none;
                    min-width: 50px;
                }}
                .switch-status {{
                    display: inline-flex;
                    align-items: center;
                    gap: 6px;
                    padding: 5px 14px;
                    border-radius: 14px;
                    font-weight: 600;
                    font-size: 0.82rem;
                    transition: var(--transition);
                    letter-spacing: 0.3px;
                }}
                .switch-status.off {{
                    background: #f5f5f8;
                    color: #888;
                }}
                .switch-status.on {{
                    background: #e6faf2;
                    color: #007a52;
                }}
                .switch-indicator {{
                    width: 9px;
                    height: 9px;
                    border-radius: 50%;
                    display: inline-block;
                }}
                .switch-indicator.off {{
                    background: #bbb;
                }}
                .switch-indicator.on {{
                    background: var(--success);
                    box-shadow: 0 0 8px rgba(0, 184, 148, 0.5);
                }}

                .record-btn {{
                    display: inline-flex;
                    align-items: center;
                    gap: 6px;
                    padding: 8px 16px;
                    border-radius: 6px;
                    background: #fff;
                    border: 1px solid #ccc;
                    cursor: pointer;
                    font-weight: 500;
                    font-size: 0.85rem;
                    transition: var(--transition);
                    user-select: none;
                }}
                .record-btn:hover {{
                    background: #f0f0f8;
                    border-color: #a0a0d0;
                }}
                .record-btn.recording {{
                    background: #ffebee;
                    border-color: #e57373;
                    color: #c62828;
                    animation: pulse-record 1s infinite;
                }}
                @keyframes pulse-record {{
                    0%,
                    100% {{
                        opacity: 1;
                    }}
                    50% {{
                        opacity: 0.7;
                    }}
                }}

                .numpad-btns {{
                    display: flex;
                    gap: 10px;
                    margin-top: 12px;
                }}
                .hotkey-list {{
                    list-style: none;
                    display: flex;
                    flex-direction: column;
                    gap: 8px;
                }}
                .hotkey-list-item {{
                    display: flex;
                    align-items: center;
                    gap: 12px;
                    padding: 10px 14px;
                    border-radius: var(--radius-sm);
                    background: #fafbfc;
                    border: 1px solid #eef0f4;
                    font-size: 0.88rem;
                    transition: var(--transition);
                    flex-wrap: wrap;
                }}
                .hotkey-list-item .hk-name {{
                    font-weight: 600;
                    min-width: 90px;
                    color: #444;
                }}
                .hotkey-list-item .hk-combo {{
                    font-family: var(--font-mono);
                    font-size: 0.82rem;
                    background: #f0f1f5;
                    padding: 3px 10px;
                    border-radius: 5px;
                    letter-spacing: 0.3px;
                    color: var(--accent);
                    font-weight: 500;
                }}

                .toast-container {{
                    position: fixed;
                    top: 24px;
                    right: 24px;
                    z-index: 9999;
                    display: flex;
                    flex-direction: column;
                    gap: 10px;
                    pointer-events: none;
                }}
                .toast {{
                    pointer-events: auto;
                    padding: 12px 18px;
                    border-radius: 8px;
                    background: #1a1a2e;
                    color: #fff;
                    font-size: 0.85rem;
                    font-weight: 500;
                    box-shadow: 0 8px 28px rgba(0, 0, 0, 0.22);
                    animation: toastIn 0.35s ease-out, toastOut 0.3s 2.2s ease-in forwards;
                    max-width: 380px;
                    letter-spacing: 0.2px;
                    display: flex;
                    align-items: center;
                    gap: 8px;
                }}
                .toast .toast-icon {{
                    font-size: 1.1rem;
                    flex-shrink: 0;
                }}
                @keyframes toastIn {{
                    from {{
                        opacity: 0;
                        transform: translateX(60px);
                    }}
                    to {{
                        opacity: 1;
                        transform: translateX(0);
                    }}
                }}
                @keyframes toastOut {{
                    from {{
                        opacity: 1;
                        transform: translateX(0);
                    }}
                    to {{
                        opacity: 0;
                        transform: translateX(40px);
                    }}
                }}

                .repeat-indicator {{
                    display: inline-block;
                    padding: 3px 8px;
                    border-radius: 4px;
                    font-size: 0.7rem;
                    font-weight: 600;
                    background: #ffeaa7;
                    color: #d63031;
                    margin-left: 8px;
                    opacity: 0;
                    transition: opacity 0.2s;
                }}
                .repeat-indicator.visible {{
                    opacity: 1;
                }}

                @media (max-width: 600px) {{
                    .card {{
                        padding: 18px 16px;
                    }}
                    .card-header {{
                        flex-wrap: wrap;
                        gap: 8px;
                    }}
                    .demo-btn {{
                        width: 100%;
                        justify-content: center;
                    }}
                    .switch-row {{
                        gap: 10px;
                    }}
                    .hotkey-list-item {{
                        flex-direction: column;
                        align-items: flex-start;
                        gap: 6px;
                    }}
                }}
            </style>
        </head>
        <body>

            <div class="container">
                <div class="header">
                    <div class="icon-wrapper">⌨️</div>
                    <h1>HotkeyManager v3.4</h1>
                    <p class="subtitle">全局快捷键管理器 · 防重复触发 · 忽略repeat事件</p>
                    <span class="platform-badge" id="platformBadge"></span>
                </div>

                <!-- 示例1：按钮快捷键 (展示repeat过滤) -->
                <div class="card" id="btnCard">
                    <div class="card-header">
                        <div class="card-icon btn-icon">🔘</div>
                        <span class="card-title">示例一：按钮快捷键 (防重复触发)</span>
                        <span class="card-badge"><span class="key-symbol" id="btnHotkeyDisplay">Ctrl + Alt + B</span></span>
                    </div>
                    <p style="color:var(--text-secondary);font-size:0.88rem;margin-bottom:16px;">
                        按住快捷键不放，只触发一次。<span class="repeat-indicator" id="repeatIndicator">⏱️ 重复事件已过滤</span>
                    </p>
                    <div style="display:flex;align-items:center;gap:14px;flex-wrap:wrap;">
                        <button class="demo-btn" id="demoButton"><span>⚡</span> 点击执行</button>
                        <span class="click-count" id="clickCount">0</span>
                        <span style="font-size:0.8rem;color:#999;">次操作</span>
                    </div>
                    <p style="margin-top:12px;font-size:0.78rem;color:#aaa;" id="btnHint"></p>
                </div>

                <!-- 示例2：Switch开关 -->
                <div class="card" id="switchCard">
                    <div class="card-header">
                        <div class="card-icon switch-icon">🔀</div>
                        <span class="card-title">示例二：Switch 开关快捷键</span>
                        <span class="card-badge"><span class="key-symbol" id="switchHotkeyDisplay">Ctrl + Alt + S</span></span>
                    </div>
                    <p style="color:var(--text-secondary);font-size:0.88rem;margin-bottom:16px;">
                        按下快捷键或点击开关，执行相同的切换操作。
                    </p>
                    <div class="switch-row">
                        <div class="switch-wrapper" id="demoSwitch" role="switch" aria-checked="false" tabindex="0">
                            <div class="switch-track"></div>
                            <div class="switch-thumb"></div>
                        </div>
                        <span class="switch-label">暗色模式</span>
                        <span class="switch-status off" id="switchStatus"><span class="switch-indicator off"></span> 关闭</span>
                    </div>
                    <p style="margin-top:12px;font-size:0.78rem;color:#aaa;" id="switchHint"></p>
                </div>

                <!-- 示例3：自定义快捷键 -->
                <div class="card" id="customCard">
                    <div class="card-header">
                        <div class="card-icon custom-icon">⚙️</div>
                        <span class="card-title">示例三：动态自定义快捷键</span>
                    </div>
                    <p style="color:var(--text-secondary);font-size:0.88rem;margin-bottom:16px;">
                        点击录制按钮，按下任意组合键（包括小键盘按键）来改变按钮的快捷键。
                    </p>
                    <div style="display:flex;align-items:center;gap:16px;flex-wrap:wrap;margin-bottom:16px;">
                        <button class="demo-btn" id="customButton"><span>🎯</span> 自定义按钮</button>
                        <span style="font-weight:500;">当前快捷键：</span>
                        <span class="card-badge"><span class="key-symbol" id="customHotkeyText">Ctrl + Alt + K</span></span>
                    </div>
                    <div>
                        <button class="record-btn" id="recordBtn">🎤 录制新快捷键</button>
                        <span id="recordStatus" style="margin-left:12px;font-size:0.85rem;color:#888;"></span>
                    </div>
                    <p style="margin-top:12px;font-size:0.78rem;color:#aaa;" id="customHint"></p>
                </div>

                <!-- 示例4：数字小键盘增减 -->
                <div class="card" id="numpadCard">
                    <div class="card-header">
                        <div class="card-icon numpad-icon">🔢</div>
                        <span class="card-title">示例四：小键盘增减 (含模拟按钮)</span>
                        <span class="card-badge"><span class="key-symbol" id="numpadHotkeyDisplay">Numpad + / -</span></span>
                    </div>
                    <p style="color:var(--text-secondary);font-size:0.88rem;margin-bottom:16px;">
                        使用小键盘的 <b>+</b> 键增加计数，<b>-</b> 键减少计数。
                    </p>
                    <div style="display:flex;align-items:center;gap:16px;flex-wrap:wrap;">
                        <span style="font-weight:500;">计数器：</span>
                        <span class="click-count" id="numpadCounter">0</span>
                        <button class="demo-btn" id="resetNumpadBtn" style="background:#636e72;">↺ 重置</button>
                    </div>
                    <div class="numpad-btns">
                        <button class="demo-btn" id="simNumpadPlus" style="background:#00b894;">➕ 模拟小键盘 +</button>
                        <button class="demo-btn" id="simNumpadMinus" style="background:#e17055;">➖ 模拟小键盘 -</button>
                    </div>
                    <p style="margin-top:12px;font-size:0.78rem;color:#aaa;" id="numpadHint"></p>
                </div>

                <!-- 已注册快捷键列表 -->
                <div class="card" id="registryCard">
                    <div class="card-header">
                        <div class="card-icon" style="background:#f5f0ff;">📋</div>
                        <span class="card-title">已注册的快捷键</span>
                        <span style="font-size:0.78rem;color:#999;" id="registryCount">0 个</span>
                    </div>
                    <ul class="hotkey-list" id="hotkeyList">
                        <li class="empty-state">暂无注册的快捷键</li>
                    </ul>
                    <div style="margin-top:14px;display:flex;gap:10px;flex-wrap:wrap;">
                        <button
                            id="btnPause"
                            style="
                                padding:8px 16px;
                                border-radius:6px;
                                border:1px solid #ddd;
                                background:#fff;
                                cursor:pointer;
                                font-size:0.84rem;
                                font-weight:500;
                                transition:var(--transition);
                            "
                        >⏸️ 暂停管理器
                        </button>
                        <button
                            id="btnClearAll"
                            style="
                                padding:8px 16px;
                                border-radius:6px;
                                border:1px solid #f0d0d0;
                                background:#fff5f5;
                                color:#c0392b;
                                cursor:pointer;
                                font-size:0.84rem;
                                font-weight:500;
                                transition:var(--transition);
                            "
                        >🗑️ 清除全部
                        </button>
                        <button
                            id="btnReRegister"
                            style="
                                padding:8px 16px;
                                border-radius:6px;
                                border:1px solid #ddd;
                                background:#fff;
                                cursor:pointer;
                                font-size:0.84rem;
                                font-weight:500;
                                transition:var(--transition);
                            "
                        >🔄 重新注册示例
                        </button>
                    </div>
                </div>
            </div>

            <div class="toast-container" id="toastContainer"></div>

            <script>
                (function() {{
                    {hkManager_str}

                    // ==================== 工具函数 ====================
                    function detectPlatform() {{
                        const ua = navigator.userAgent || '';
                        if (/mac/i.test(navigator.platform) || /macintosh/i.test(ua)) return 'mac';
                        if (/win/i.test(navigator.platform) || /windows/i.test(ua)) return 'windows';
                        return 'other';
                    }}

                    function beautifyHotkey(hotkeyString, platform) {{
                        const parts = hotkeyString.trim().toLowerCase().split('+').map(s => s.trim());
                        const key = parts.pop();
                        const keyDisplay = (() => {{
                            const displayMap = {{
                                'arrowup': '↑',
                                'arrowdown': '↓',
                                'arrowleft': '←',
                                'arrowright': '→',
                                ' ': 'Space',
                                'enter': 'Enter',
                                'escape': 'Esc',
                                'backspace': 'Backspace',
                                'tab': 'Tab',
                                'delete': 'Del',
                                'insert': 'Ins',
                                'home': 'Home',
                                'end': 'End',
                                'pageup': 'PgUp',
                                'pagedown': 'PgDn',
                                'numpad0': 'Numpad 0',
                                'numpad1': 'Numpad 1',
                                'numpad2': 'Numpad 2',
                                'numpad3': 'Numpad 3',
                                'numpad4': 'Numpad 4',
                                'numpad5': 'Numpad 5',
                                'numpad6': 'Numpad 6',
                                'numpad7': 'Numpad 7',
                                'numpad8': 'Numpad 8',
                                'numpad9': 'Numpad 9',
                                'numpaddecimal': 'Numpad .',
                                'numpaddivide': 'Numpad /',
                                'numpadmultiply': 'Numpad *',
                                'numpadsubtract': 'Numpad -',
                                'numpadadd': 'Numpad +',
                                'numpadenter': 'Numpad Enter',
                            }};
                            const lower = key.toLowerCase();
                            if (displayMap[lower]) return displayMap[lower];
                            if (key.startsWith('numpad')) return key.charAt(0).toUpperCase() + key.slice(1);
                            if (/^f\d+$/i.test(key)) return key.toUpperCase();
                            return key.length === 1 ? key.toUpperCase() : key;
                        }})();
                        const symbols = {{
                            ctrl: platform === 'mac' ? '⌃' : 'Ctrl',
                            alt: platform === 'mac' ? '⌥' : 'Alt',
                            shift: platform === 'mac' ? '⇧' : 'Shift',
                            command: platform === 'mac' ? '⌘' : 'Win',
                            meta: platform === 'mac' ? '⌘' : 'Win',
                            option: platform === 'mac' ? '⌥' : 'Alt',
                        }};
                        const modDisplay = parts.map(p => symbols[p] || p);
                        return platform === 'mac' ? [...modDisplay, keyDisplay].join(' ') : [...modDisplay, keyDisplay].join(' + ');
                    }}

                    function showToast(message, icon = '⚡') {{
                        const container = document.getElementById('toastContainer');
                        const toast = document.createElement('div');
                        toast.className = 'toast';
                        toast.innerHTML = `<span class="toast-icon">${{icon}}</span> ${{message}}`;
                        container.appendChild(toast);
                        setTimeout(() => {{ if (toast.parentNode) toast.remove(); }}, 2600);
                    }}

                    // ==================== 应用初始化 ====================
                    const platform = detectPlatform();
                    document.getElementById('platformBadge').textContent =
                        platform === 'mac' ? '🍎 macOS · Command=⌘ Option=⌥' :
                        platform === 'windows' ? '🪟 Windows · Ctrl / Alt / Win' : '💻 通用平台';

                    // 创建HotkeyManager实例 (默认启用ignoreRepeat)
                    const hotkeyManager = new HotkeyManager({{
                        ignoreEditable: true,
                        preventDefault: true,
                        stopPropagation: true,
                        ignoreRepeat: true, // ✅ 默认忽略重复事件
                        debug: false,
                    }});

                    // ---- 示例1：按钮快捷键 (含repeat反馈) ----
                    const demoButton = document.getElementById('demoButton');
                    const clickCountEl = document.getElementById('clickCount');
                    const btnCard = document.getElementById('btnCard');
                    const btnHotkeyDisplay = document.getElementById('btnHotkeyDisplay');
                    const btnHint = document.getElementById('btnHint');
                    const repeatIndicator = document.getElementById('repeatIndicator');
                    const BTN_HOTKEY = platform === 'mac' ? 'command+alt+b' : 'ctrl+alt+b';
                    btnHotkeyDisplay.textContent = beautifyHotkey(BTN_HOTKEY, platform);
                    btnHint.textContent = `按下 ${{beautifyHotkey(BTN_HOTKEY, platform)}} 或点击按钮 (按住不放只触发一次)`;
                    let clickCount = 0;

                    // 全局回调：当repeat事件被过滤时显示指示器
                    window._onHotkeyRepeatBlocked = function(hotkey) {{
                        if (hotkey === 'alt+ctrl+b' || hotkey === 'meta+alt+b') {{
                            repeatIndicator.classList.add('visible');
                            setTimeout(() => repeatIndicator.classList.remove('visible'), 600);
                        }}
                    }};

                    function handleButtonAction(source = 'click') {{
                        clickCount++;
                        clickCountEl.textContent = clickCount;
                        clickCountEl.classList.remove('bump');
                        void clickCountEl.offsetWidth;
                        clickCountEl.classList.add('bump');
                        demoButton.classList.remove('triggered');
                        void demoButton.offsetWidth;
                        demoButton.classList.add('triggered');
                        btnCard.classList.remove('flash');
                        void btnCard.offsetWidth;
                        btnCard.classList.add('flash');
                        showToast(`按钮操作触发！(${{source === 'hotkey' ? '快捷键' : '点击'}}) 总计: ${{clickCount}} 次`, '🔘');
                    }}
                    demoButton.addEventListener('click', () => handleButtonAction('click'));
                    hotkeyManager.register(demoButton, BTN_HOTKEY, (e) => handleButtonAction('hotkey'), {{
                        description: '按钮点击 (防重复)',
                        ignoreEditable: false,
                        ignoreRepeat: true, // 明确忽略重复
                    }});

                    // ---- 示例2：Switch开关 ----
                    const demoSwitch = document.getElementById('demoSwitch');
                    const switchStatus = document.getElementById('switchStatus');
                    const switchCard = document.getElementById('switchCard');
                    const switchHotkeyDisplay = document.getElementById('switchHotkeyDisplay');
                    const switchHint = document.getElementById('switchHint');
                    const SWITCH_HOTKEY = platform === 'mac' ? 'command+alt+s' : 'ctrl+alt+s';
                    switchHotkeyDisplay.textContent = beautifyHotkey(SWITCH_HOTKEY, platform);
                    switchHint.textContent = `按下 ${{beautifyHotkey(SWITCH_HOTKEY, platform)}} 或点击开关`;
                    let isSwitchOn = false;

                    function updateSwitchUI() {{
                        if (isSwitchOn) {{
                            demoSwitch.classList.add('on');
                            demoSwitch.setAttribute('aria-checked', 'true');
                            switchStatus.className = 'switch-status on';
                            switchStatus.innerHTML = '<span class="switch-indicator on"></span> 开启';
                        }} else {{
                            demoSwitch.classList.remove('on');
                            demoSwitch.setAttribute('aria-checked', 'false');
                            switchStatus.className = 'switch-status off';
                            switchStatus.innerHTML = '<span class="switch-indicator off"></span> 关闭';
                        }}
                    }}

                    function handleSwitchToggle(source = 'click') {{
                        isSwitchOn = !isSwitchOn;
                        updateSwitchUI();
                        switchCard.classList.remove('flash');
                        void switchCard.offsetWidth;
                        switchCard.classList.add('flash');
                        showToast(`暗色模式 ${{isSwitchOn ? '🌙 开启' : '☀️ 关闭'}}（${{source === 'hotkey' ? '快捷键' : '点击'}}）`,
                            isSwitchOn ? '🌙' : '☀️');
                    }}
                    demoSwitch.addEventListener('click', () => handleSwitchToggle('click'));
                    demoSwitch.addEventListener('keydown', (e) => {{
                        if (e.key === 'Enter' || e.key === ' ') {{ e.preventDefault();
                            handleSwitchToggle('click'); }}
                    }});
                    hotkeyManager.register(demoSwitch, SWITCH_HOTKEY, (e) => handleSwitchToggle('hotkey'), {{
                        description: '暗色模式开关',
                        ignoreEditable: false,
                    }});

                    // ---- 示例3：自定义快捷键 ----
                    const customButton = document.getElementById('customButton');
                    const customHotkeyText = document.getElementById('customHotkeyText');
                    const recordBtn = document.getElementById('recordBtn');
                    const recordStatus = document.getElementById('recordStatus');
                    const customCard = document.getElementById('customCard');
                    const customHint = document.getElementById('customHint');
                    let customHotkey = platform === 'mac' ? 'command+alt+k' : 'ctrl+alt+k';
                    let customUnregister = null;

                    function updateCustomHotkeyDisplay() {{
                        customHotkeyText.textContent = beautifyHotkey(customHotkey, platform);
                        customHint.textContent = `当前自定义快捷键：${{beautifyHotkey(customHotkey, platform)}}`;
                    }}

                    function registerCustomHotkey(hotkeyStr) {{
                        if (customUnregister) customUnregister();
                        customUnregister = hotkeyManager.register(customButton, hotkeyStr, (e) => {{
                            customCard.classList.remove('flash');
                            void customCard.offsetWidth;
                            customCard.classList.add('flash');
                            showToast(`自定义快捷键触发！(${{beautifyHotkey(hotkeyStr, platform)}})`, '🎯');
                        }}, {{ description: '自定义按钮', ignoreEditable: false }});
                    }}
                    registerCustomHotkey(customHotkey);
                    updateCustomHotkeyDisplay();

                    let isRecording = false;
                    let recordHandler = null;
                    recordBtn.addEventListener('click', () => isRecording ? stopRecording() : startRecording());

                    function startRecording() {{
                        isRecording = true;
                        recordBtn.textContent = '⏹️ 停止录制';
                        recordBtn.classList.add('recording');
                        recordStatus.textContent = '请按下您想要的组合键...';
                        hotkeyManager.pause();
                        recordHandler = (e) => {{
                            if (['Control', 'Alt', 'Shift', 'Meta'].includes(e.key)) return;
                            e.preventDefault();
                            e.stopPropagation();
                            const mods = [];
                            if (e.ctrlKey) mods.push('ctrl');
                            if (e.altKey) mods.push('alt');
                            if (e.shiftKey) mods.push('shift');
                            if (e.metaKey) mods.push('command');
                            let rawKey = e.key;
                            if (e.code && e.code.startsWith('Numpad')) {{
                                rawKey = e.code.replace('Numpad', 'numpad').toLowerCase();
                            }}
                            const comboParts = [...mods, rawKey.toLowerCase()];
                            const newHotkey = comboParts.join('+');
                            try {{
                                hotkeyManager._parseHotkeyString(newHotkey);
                            }} catch (err) {{
                                showToast('无效的快捷键，请重试', '⚠️');
                                stopRecording();
                                return;
                            }}
                            customHotkey = newHotkey;
                            registerCustomHotkey(customHotkey);
                            updateCustomHotkeyDisplay();
                            showToast(`快捷键已更新为 ${{beautifyHotkey(customHotkey, platform)}}`, '✅');
                            stopRecording();
                        }};
                        document.addEventListener('keydown', recordHandler, true);
                    }}

                    function stopRecording() {{
                        if (!isRecording) return;
                        isRecording = false;
                        recordBtn.textContent = '🎤 录制新快捷键';
                        recordBtn.classList.remove('recording');
                        recordStatus.textContent = '';
                        hotkeyManager.resume();
                        if (recordHandler) {{
                            document.removeEventListener('keydown', recordHandler, true);
                            recordHandler = null;
                        }}
                    }}

                    // ---- 示例4：数字小键盘增减 ----
                    const numpadCounterEl = document.getElementById('numpadCounter');
                    const numpadCard = document.getElementById('numpadCard');
                    const numpadHint = document.getElementById('numpadHint');
                    const resetNumpadBtn = document.getElementById('resetNumpadBtn');
                    let numpadValue = 0;
                    const NUMPAD_PLUS_HOTKEY = 'numpad+';
                    const NUMPAD_MINUS_HOTKEY = 'numpad-';
                    document.getElementById('numpadHotkeyDisplay').textContent =
                        beautifyHotkey(NUMPAD_PLUS_HOTKEY, platform) + ' / ' + beautifyHotkey(NUMPAD_MINUS_HOTKEY, platform);
                    numpadHint.textContent = `无论NumLock开关，按下小键盘 + 增加，小键盘 - 减少`;

                    function updateNumpadDisplay() {{
                        numpadCounterEl.textContent = numpadValue;
                        numpadCounterEl.classList.remove('bump');
                        void numpadCounterEl.offsetWidth;
                        numpadCounterEl.classList.add('bump');
                        numpadCard.classList.remove('flash');
                        void numpadCard.offsetWidth;
                        numpadCard.classList.add('flash');
                    }}

                    hotkeyManager.register(numpadCard, NUMPAD_PLUS_HOTKEY, (e) => {{
                        numpadValue++;
                        updateNumpadDisplay();
                        showToast(`小键盘 + 触发，计数器：${{numpadValue}}`, '➕');
                    }}, {{ description: '小键盘增加', ignoreEditable: false }});

                    hotkeyManager.register(numpadCard, NUMPAD_MINUS_HOTKEY, (e) => {{
                        numpadValue--;
                        updateNumpadDisplay();
                        showToast(`小键盘 - 触发，计数器：${{numpadValue}}`, '➖');
                    }}, {{ description: '小键盘减少', ignoreEditable: false }});

                    document.getElementById('simNumpadPlus').addEventListener('click', () => {{
                        numpadValue++;
                        updateNumpadDisplay();
                        showToast(`模拟小键盘 + ，计数器：${{numpadValue}}`, '➕');
                    }});
                    document.getElementById('simNumpadMinus').addEventListener('click', () => {{
                        numpadValue--;
                        updateNumpadDisplay();
                        showToast(`模拟小键盘 - ，计数器：${{numpadValue}}`, '➖');
                    }});
                    resetNumpadBtn.addEventListener('click', () => {{
                        numpadValue = 0;
                        updateNumpadDisplay();
                        showToast('计数器已重置', '↺');
                    }});

                    // ---- 注册列表展示 ----
                    const hotkeyListEl = document.getElementById('hotkeyList');
                    const registryCountEl = document.getElementById('registryCount');

                    function renderRegistryList() {{
                        const hotkeys = hotkeyManager.getRegisteredHotkeys();
                        registryCountEl.textContent = hotkeys.length + ' 个';
                        if (hotkeys.length === 0) {{
                            hotkeyListEl.innerHTML = '<li class="empty-state">暂无注册的快捷键</li>';
                            return;
                        }}
                        hotkeyListEl.innerHTML = hotkeys.map(hk => {{
                            const displayCombo = beautifyHotkey(hk.hotkeyString, platform);
                            const desc = hk.descriptions ? hk.descriptions.join(', ') : '—';
                            return `<li class="hotkey-list-item">
                                <span class="hk-name">${{desc}}</span>
                                <span class="hk-combo">${{displayCombo}}</span>
                                <span style="font-size:0.75rem;color:#999;">(${{hk.count}}个handler)</span>
                            </li>`;
                        }}).join('');
                    }}
                    renderRegistryList();
                    setInterval(renderRegistryList, 2000);

                    // ---- 控制按钮 ----
                    document.getElementById('btnPause').addEventListener('click', function() {{
                        if (hotkeyManager.isPaused()) {{
                            hotkeyManager.resume();
                            this.textContent = '⏸️ 暂停管理器';
                            showToast('管理器已恢复', '▶️');
                        }} else {{
                            hotkeyManager.pause();
                            this.textContent = '▶️ 恢复管理器';
                            showToast('管理器已暂停', '⏸️');
                        }}
                    }});

                    document.getElementById('btnClearAll').addEventListener('click', () => {{
                        const count = hotkeyManager.unregisterAll();
                        clickCount = 0;
                        clickCountEl.textContent = '0';
                        isSwitchOn = false;
                        updateSwitchUI();
                        customUnregister = null;
                        numpadValue = 0;
                        updateNumpadDisplay();
                        renderRegistryList();
                        showToast(`已清除全部 ${{count}} 个快捷键`, '🗑️');
                    }});

                    document.getElementById('btnReRegister').addEventListener('click', () => {{
                        hotkeyManager.unregisterAll();
                        hotkeyManager.register(
                            demoButton,
                            BTN_HOTKEY,
                            (e) => handleButtonAction('hotkey'), {{ description: '按钮点击', ignoreEditable: false }}
                        );
                        hotkeyManager.register(
                            demoSwitch,
                            SWITCH_HOTKEY,
                            (e) => handleSwitchToggle('hotkey'), {{ description: '暗色模式开关', ignoreEditable: false }}
                        );
                        registerCustomHotkey(customHotkey);
                        hotkeyManager.register(
                            numpadCard,
                            NUMPAD_PLUS_HOTKEY,
                            (e) => {{ numpadValue++; updateNumpadDisplay(); }}, {{ description: '小键盘增加', ignoreEditable: false }}
                        );
                        hotkeyManager.register(
                            numpadCard,
                            NUMPAD_MINUS_HOTKEY,
                            (e) => {{ numpadValue--; updateNumpadDisplay(); }}, {{ description: '小键盘减少', ignoreEditable: false }}
                        );
                        clickCount = 0;
                        clickCountEl.textContent = '0';
                        isSwitchOn = false;
                        updateSwitchUI();
                        numpadValue = 0;
                        updateNumpadDisplay();
                        updateCustomHotkeyDisplay();
                        renderRegistryList();
                        showToast('示例快捷键已重新注册', '🔄');
                    }});

                    window.addEventListener('beforeunload', () => {{
                        stopRecording();
                        hotkeyManager.destroy();
                    }});

                    console.log('✅ HotkeyManager v3.4 已就绪 (防重复触发: 默认忽略event.repeat)');
                    console.log('registered hotkeys:', hotkeyManager.getRegisteredHotkeys());

                }})();
            </script>
        </body>
        </html>
    """)

    #380. Dump the script into the App file
    with open(dst_html, 'w', encoding = 'utf-8') as f:
        f.write(re.sub(r'\n\s+\n', r'\n\n', html_snippet, flags = re.M))

    #370. Test steps
    #[01] Double click to open the file <dst_html>
    #[02] Test the cases as instructed on the page
    #[03] Close the test page in the web browser

    #390. Clean the slate
    #[ASSUMPTION]
    #[1] Below action will NOT remove its parent folders
    shutil.rmtree(dst_dir, ignore_errors = True)

    """
#-Notes- -End-
'''
